<%= form_for @player, :url => url_for(:controller => 'players', :action => 'verify') do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>

	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.submit "Submit" %>
<% end  %>
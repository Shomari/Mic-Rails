<%= form_for(@player) do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>

	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.label "confirm password" %>
	<%= f.password_field :nil %>

	<%= f.submit "Submit" %>
<% end  %>
<div class="container">
	<div class="row" id="headliner">
		<div class="col-md-8">
			<h1 id="tagline">Never Play Alone</h1>
			<ul id="main-text">
				<li>Find gamers online now that use headsets</li>
				<li>Have more fun playing mutiplayer games</li>
			</ul>
		</div>
		<div class="col-md-3">
			<div class="row" id="count">
				<div class="col-md-12">
					<p><%= @sessions.count  %> players currently online.</p>
				</div>
			</div>
		</div>
	</div>

	<div class="row">
		<div class="col-md-4 col-md-offset-4 text-center" id="login">
			<%= form_for @player, :url => player_session_path, html: { class: "form-signin"} do |f| %>
				<h2 class="form-signin-heading">Please sign in</h2>
				<%= f.label :email, :class => "sr-only" %>
				<%= f.text_field :email, :class => "form-control", :placeholder => "Email" %>

				<%= f.label :password, :class => "sr-only" %>
				<%= f.password_field :password, :class => "form-control", :placeholder => "Password" %>

				<%= f.submit "Submit", :class => "btn btn-lg btn-primary btn-block" %>
			<% end  %>
		</div>
	</div>
</div>